/**
 * Hero / bio RichText: thinner body (font-light) while keeping hierarchy readable.
 * Styles live in globals.css as `.hero-bio-richtext` (no !important).
 */
export const heroBioRichTextClassName = 'hero-bio-richtext'
